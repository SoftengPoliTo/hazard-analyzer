#![deny(missing_docs, unsafe_code)]

//! `code-certifier` defines some utility functions
//! and data structures shared between `hazard-analyzer` and `pub-api`.

pub mod error;
pub mod git;
pub mod manifest;